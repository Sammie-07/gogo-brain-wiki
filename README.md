# Gogo's Brain — Wiki (public mirror)

This repo is a **public, read-only mirror of the `wiki/` notes** from Gogo Bethke's knowledge base.
It exists for one purpose: to power the live "Gogo's Brain" graph view (`gogo-brain-live.html`),
which reads these Markdown notes in the browser and renders an Obsidian-style graph from the
`[[wikilinks]]` between them.

- **Source of truth:** the private vault (`gogo-bethke-brain`). This mirror is auto-synced from it.
- **Contents:** curated entity, concept, and source notes only — no raw transcripts.
- Do not edit here; edits happen in the vault and sync downstream.
